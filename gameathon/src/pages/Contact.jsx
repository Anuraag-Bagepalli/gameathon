import { useState } from 'react'; import { MapPin, Phone, Send } from 'lucide-react'; import { API_URL } from '../config';
const initial={name:'',email:'',subject:'',message:''};
const contacts=[['Prof. Jayanth K','Faculty Coordinator','+91 9731937147'],['Sathvik Vasishta','Captain Code','+91 9606018932'],['Priyanka T S','Friday','+91 7349710985']];
export default function Contact(){const [form,setForm]=useState(initial),[state,setState]=useState({busy:false,text:'',error:false});const submit=async e=>{e.preventDefault();setState({busy:true,text:'',error:false});try{const r=await fetch(`${API_URL}/api/contact`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw new Error(d.error||d.message||'Message failed');setForm(initial);setState({busy:false,text:'Message received. The Gameathon team will get back to you.',error:false});}catch(err){setState({busy:false,text:err.message,error:true})}};return <main className="page-shell"><div className="container"><header className="page-hero"><span className="kicker">// CONTACT THE TEAM</span><h1>Let's talk.</h1><p>Questions about Gameathon 8.0, participation, partnerships, or the venue? Reach the organizing team directly.</p></header><div className="contact-layout"><div><article className="card venue-card"><MapPin/><h3>Event venue</h3><p>Jyothy Institute of Technology<br/>Thataguni, Bengaluru, Karnataka</p><span>2026 · DATE COMING SOON</span></article><div className="contact-list">{contacts.map(([name,role,phone])=><a className="contact-person" href={`tel:${phone.replace(/\s/g,'')}`} key={phone}><div><strong>{name}</strong><span>{role}</span></div><div><Phone size={16}/>{phone}</div></a>)}</div></div><form className="card" onSubmit={submit}>{state.text&&<div className={`form-message ${state.error?'error':'success'}`}>{state.text}</div>}<div className="form-row"><Field label="Name" name="name" value={form.name} set={setForm}/><Field label="Email" name="email" type="email" value={form.email} set={setForm}/></div><Field label="Subject" name="subject" value={form.subject} set={setForm}/><div className="form-group"><label>Message</label><textarea required rows="6" value={form.message} onChange={e=>setForm(f=>({...f,message:e.target.value}))}/></div><button className="btn btn-primary" disabled={state.busy}><Send size={17}/>{state.busy?'Sending...':'Send message'}</button></form></div></div></main>}
function Field({label,set,...props}){return <div className="form-group"><label>{label}</label><input required onChange={e=>set(f=>({...f,[e.target.name]:e.target.value}))} {...props}/></div>}

