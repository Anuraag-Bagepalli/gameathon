import { useState } from 'react'; import { CheckCircle2, LoaderCircle, Users } from 'lucide-react'; import { API_URL } from '../config';
const initial={teamName:'',teamLeader:'',email:'',phone:'',college:'',participationType:'offline',trainingOption:'not-applicable',memberCount:'1',teamMembers:['']};
export default function Registration(){const [form,setForm]=useState(initial),[state,setState]=useState({busy:false,type:'',text:''});const change=e=>{const {name,value}=e.target;if(name==='memberCount'){const count=Number(value);setForm(f=>({...f,memberCount:value,teamMembers:Array.from({length:count},(_,i)=>f.teamMembers[i]||'')}))}else setForm(f=>({...f,[name]:value}))};const member=(i,value)=>setForm(f=>({...f,teamMembers:f.teamMembers.map((m,n)=>n===i?value:m)}));const submit=async e=>{e.preventDefault();setState({busy:true,type:'',text:''});try{const fd=new FormData();Object.entries(form).forEach(([k,v])=>fd.append(k,k==='teamMembers'?JSON.stringify(v):v));const r=await fetch(`${API_URL}/api/register`,{method:'POST',body:fd});const d=await r.json();if(!r.ok)throw new Error(d.error||d.message||'Registration failed');setState({busy:false,type:'success',text:`Interest registered. Reference: ${d.registration?._id||'received'}`});setForm(initial)}catch(err){setState({busy:false,type:'error',text:err.message})}};return <main className="page-shell"><div className="container"><header className="page-hero"><span className="kicker">// GAMEATHON 8.0 Â· 2026</span><h1>Build with us.</h1><p>Register your interest now. Final event dates, participation rules, and challenge details will be shared when announced.</p></header><form className="registration-form card" onSubmit={submit}>{state.text&&<div className={`form-message ${state.type}`}>{state.text}</div>}<div className="form-row"><Field label="Team name" name="teamName" value={form.teamName} onChange={change}/><Field label="Team lead" name="teamLeader" value={form.teamLeader} onChange={change}/></div><div className="form-row"><Field label="Email" name="email" type="email" value={form.email} onChange={change}/><Field label="Phone" name="phone" type="tel" pattern="[0-9+ -]{10,15}" value={form.phone} onChange={change}/></div><Field label="College / organization" name="college" value={form.college} onChange={change}/><Select label="Team size" name="memberCount" value={form.memberCount} onChange={change} options={[[1,'1 creator'],[2,'2 creators'],[3,'3 creators'],[4,'4 creators']]}/><div className="form-row">{form.teamMembers.map((m,i)=><Field key={i} label={`Creator ${i+1}`} value={m} onChange={e=>member(i,e.target.value)}/>)}</div><div className="interest-note"><Users/><div><strong>Registration of interest</strong><p>No payment is requested on this form. Full participation details will follow with the official date announcement.</p></div></div><label className="consent"><input type="checkbox" required/> I confirm that these contact and team details are accurate.</label><button className="btn btn-primary" style={{width:'100%',marginTop:22}} disabled={state.busy}>{state.busy?<><LoaderCircle className="spin" size={18}/> Sending...</>:<><CheckCircle2 size={18}/> Register interest</>}</button></form></div></main>}
function Field({label,...props}){return <div className="form-group"><label>{label}</label><input required autoComplete="off" {...props}/></div>};function Select({label,options,...props}){return <div className="form-group"><label>{label}</label><select required {...props}>{options.map(([v,t])=><option key={v} value={v}>{t}</option>)}</select></div>}

